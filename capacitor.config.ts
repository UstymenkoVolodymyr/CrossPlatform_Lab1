import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lr1',
  appName: 'lr1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
